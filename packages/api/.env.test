# Public-safe synthetic test environment.
# This file is tracked for local/test defaults only. Do not place real service
# credentials here; use an untracked local override or the approved secret store.
DATABASE_URL=postgresql://coforma_test:coforma_test@localhost:5432/coforma_test
NODE_ENV=test
