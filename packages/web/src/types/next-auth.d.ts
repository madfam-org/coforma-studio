/**
 * DEPRECATED: NextAuth.js type augmentations are no longer used.
 *
 * Session types are now defined in @/lib/auth.ts as AppSession and SessionUser.
 * This file is kept empty to avoid TypeScript errors from any stale imports.
 * Safe to delete once all references to 'next-auth' module augmentations are confirmed removed.
 */
export {};
