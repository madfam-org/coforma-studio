apiVersion: apps/v1
kind: Deployment
metadata:
  name: coforma-studio-web
  namespace: coforma-studio
  labels:
    app: coforma-studio-web
    app.kubernetes.io/name: coforma-studio-web
    app.kubernetes.io/part-of: coforma-studio
spec:
  replicas: 2
  selector:
    matchLabels:
      app: coforma-studio-web
  template:
    metadata:
      labels:
        app: coforma-studio-web
        app.kubernetes.io/name: coforma-studio-web
        app.kubernetes.io/part-of: coforma-studio
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
      - name: ghcr-credentials
      containers:
      - name: coforma-studio-web
        image: ghcr.io/madfam-org/coforma-studio/web:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
        envFrom:
        - secretRef:
            name: coforma-studio-secrets
            optional: true
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ALL]
        startupProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 30
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          periodSeconds: 30
